import React from "react";
import { ButtonWrapper } from "./GradientButtonStyles";

const GradientButton = ({ text }) => {
  return <ButtonWrapper>{text}</ButtonWrapper>;
};

export default GradientButton;
